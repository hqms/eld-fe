
  # Truck Driver Trip Planner

  This is a code bundle for Truck Driver Trip Planner. The original project is available at https://www.figma.com/design/EAQkEZS2byks2xL8O3T96P/Truck-Driver-Trip-Planner.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  